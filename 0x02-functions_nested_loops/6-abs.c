#include "main.h"

/**
 * _abs - cpmputes the absolute value of an integer
 * @n: the integer to check
 * Return: the absolute value of integer n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
