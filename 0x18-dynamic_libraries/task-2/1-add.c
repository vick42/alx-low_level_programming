#include "main.h"

/**
 * add - a function that adds two integers and returns the result.
 * @a: int type number
 * @b: int type number
 * Return: result of addition type int
 */
int add(int a, int b)
{
	return (a + b);
}
