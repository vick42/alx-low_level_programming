#include "main.h"

/**
 * add - a function that subtracts two integers and returns the result.
 * @a: int type number
 * @b: int type number
 * Return: result of addition type int
 */
int sub(inta, int b)
{
	return (a - b);
}
