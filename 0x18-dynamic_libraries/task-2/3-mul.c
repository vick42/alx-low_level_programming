#include "main.h"

/**
 * add - a function that multiply two integers and returns the result.
 * @a: int type number
 * @b: int type number
 * Return: result of addition type int
 */
int mul(int a, int b)
{
	return (a * b);
}
