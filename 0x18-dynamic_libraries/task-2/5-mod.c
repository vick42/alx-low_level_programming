#include "main.h"

/**
 * add - a function that moddulo two integers and returns the result.
 * @a: int type number
 * @b: int type number
 * Return: result of addition type int
 */
int mod(int a, int b)
{
	return (a % b);
}
